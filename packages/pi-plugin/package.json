{
	"name": "@pi-kanban/plugin",
	"version": "0.1.0",
	"type": "module",
	"description": "pi extension streaming session lifecycle, usage stats and approval gating to a pi-kanban dashboard",
	"license": "MIT",
	"files": [
		"dist"
	],
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"build": "esbuild src/index.ts --bundle --format=esm --platform=node --target=node22 --outfile=dist/pi-kanban.js --legal-comments=none && node -e \"require('node:fs').renameSync('dist/pi-kanban.js','dist/pi-kanban.ts')\"",
		"prepack": "npm run build",
		"install-ext": "node scripts/install.mjs",
		"test": "esbuild test/config.test.ts test/gate.test.ts test/project-snapshot.test.ts test/memory.test.ts test/status.test.ts --bundle --format=esm --platform=node --target=node22 --outdir=dist --out-extension:.js=.mjs --legal-comments=none && node dist/config.test.mjs && node dist/gate.test.mjs && node dist/project-snapshot.test.mjs && node dist/memory.test.mjs && node dist/status.test.mjs",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@earendil-works/pi-coding-agent": "^0.84.3",
		"@earendil-works/pi-tui": "0.84.4",
		"@pi-kanban/shared": "workspace:*",
		"@types/node": "^22.10.0",
		"esbuild": "^0.25.0",
		"typescript": "^5.7.0"
	}
}
